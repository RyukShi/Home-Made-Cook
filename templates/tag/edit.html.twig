{% extends 'base.html.twig' %}

{% block title %}Edit {{ tag.name ~ ' tag' }}{% endblock %}

{% block body %}
    <h1 class="main-title">Edit {{ tag.name ~ ' tag' }}</h1>

    {{ include('tag/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="flex gap-3 mt-4">
        <a class="btn btn-primary" href="{{ path('app_tag_index') }}">Back to list</a>
        {{ include('tag/_delete_form.html.twig') }}
    </div>
{% endblock %}
